# this workflow runs unit tests and checks code style
# for different versions of python on different operating systems

name: tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  tests:
    runs-on: ${{ matrix.config.os }}
    name: ${{ matrix.config.os }} (${{ matrix.config.py }})
    strategy:
      fail-fast: false
      matrix:
        config:
          - { os: ubuntu-latest, py: "3.14" }
          - { os: ubuntu-latest, py: "3.13" }
          - { os: ubuntu-latest, py: "3.12" }
          - { os: ubuntu-latest, py: "3.11" }
          - { os: ubuntu-latest, py: "3.10" }
          - { os: windows-latest, py: "3.10" }
          - { os: macOS-latest, py: "3.10" }

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.config.py }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.config.py }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install package
        run: uv sync

      - name: Test with pytest
        run: uv run pytest tests/ --cov=unesco_reader --cov-report=xml

      - name: Use Codecov to track coverage
        if: matrix.config.os == 'ubuntu-latest' && matrix.config.py == '3.13'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml   # coverage report
          fail_ci_if_error: true
          verbose: true

      - name: Check code style
        run: uv run black src tests --check
